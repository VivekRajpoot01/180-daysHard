#include <iostream>
using namespace std;
int main(){
    int arr[100], i, size, element, index;
    cout<<"Enter the size of array: ";
    cin>>size;
    cout<<"Enter the elements in the array: ";
    for(i=0; i<size; i++){
        cin>>arr[i];
    }
    
    return 0;
}